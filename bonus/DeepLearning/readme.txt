After building finishes launch with "docker run -it --rm name"
Then execute the executable with bash command

    "bash Miniconda2-latest-Linux-x86_64.sh"

cd into root and source the .bashrc

    "source .bashrc"

Then come back to where the executable is and perform the following

    "conda install -c anaconda jupyter"